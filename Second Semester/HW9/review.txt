sdfRenderer.h: 
- 3 класса в одном файле

- оформление списка перечисляемых подключаемых директив (несоответствие стайлгайду) 

- начиная с 109 строчки - не все private поля классов именуются с символа m

- много методов без комментариев

- в классе ImagesCache открывающую фигурную скобку стоило бы перенести на следующую строку

sdfRenderer.cpp: 
- в стайлгайде рекомендуется обрамлять тело циклов/условий фигурными скобками, даже 
если тело состоит из 1 инструкции, в строке 53 не так.

- начиная со строчки 103, разное оформление условий: 
else if(elem.tagName()=="ellipse")
	{
		ellipse(elem);
	}
	else if (elem.tagName() == "arc") {
		arc(elem);
	}
То есть перенос открывающей фигурной скобки, то его нет. 

- co 107 по 150 строчки оператор "==" не выделяется пробелами

- условия в строчках 184-201 можно было бы сделать с помощью switch

- в 260 и 264 строчках условие вновь не выделяется фигурными скобками

- можно и с помощью 1 if: 
	else if (xnum.endsWith("a") && mNeedScale)
	{
		xnum.chop(1);
		x = xnum.toFloat() + mStartX;
	}
	else if (xnum.endsWith("a") && !mNeedScale)
	{
		xnum.chop(1);
		x = xnum.toFloat() * current_size_x / first_size_x + mStartX;
	}
также, начиная со строки 372

- начиная со строки 420 много копипаста

- 640-653 - можно было через switch

- 657, 659 - опять не выделяется фигурными скобками

- 691-709 - вновь switch, также лишние пустые строки

- 768 - с табуляцией некоторые проблемы

- начиная со строки 798 проблемы с закрывающими фигурными скобками

- 870, 921 - лишние пустые строки